# 질문 프롬프트 로그

## Stage
questions

## Record ID
Zeroin 펀드평가 방법론 - only_text_structure_0005

## Question
선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.

## Model
gpt-5-nano

## API Base
https://api.openai.com/v1

## Extra
```json
{
  "chapter": "X.7. 유사펀드 측정",
  "section": "5) 선정지표",
  "subsection": "",
  "seed_title": "5) 선정지표",
  "record_target": 12,
  "requested_question_count": 14,
  "call_requested_question_count": 6,
  "call_allowed_types": [
    "definition",
    "comparison",
    "application"
  ],
  "call_type_targets": {
    "definition": 2,
    "comparison": 2,
    "application": 2
  },
  "target_total": 100,
  "generated_question": "선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.",
  "qa_type": "application",
  "question_template": "application_order"
}
```

## System Prompt
```text
당신은 제로인 방법론 기반 QA 데이터셋의 질문 생성기다.
반드시 한국어로만 답하고, 제공된 재료 범위 안에서만 질문을 만든다.
질문은 짧고 단일 쟁점 중심이어야 한다.
일반 금융상식, 투자조언, 문서 바깥 배경지식 질문은 금지한다.
업로드 문서를 보지 않고도 일반 금융 상식만으로 답할 수 있는 질문은 생성하지 않는다.
질문은 반드시 문서 안의 특정 정의, 기준, 비교, 적용 중 하나에 직접 매핑되어야 한다.
정답은 문서의 핵심 정보와 의미 연결을 위한 최소 설명만으로 성립해야 하며, 문서 밖 지식 확장을 요구하는 질문은 만들지 않는다.
정답이 자연스러운 문장으로 연결될 수는 있어도, 그 연결을 위해 새로운 정보가 필요해지는 질문은 만들지 않는다.
```

## User Prompt
```text
아래 재료만 사용해서 자연스러운 질문 후보를 생성해줘.

[단원 제목]
X.7. 유사펀드 측정

[절 제목]
5) 선정지표

[소절 제목]


[핵심 제목]
5) 선정지표

[핵심 명사]
) 선정지표, 선정지표, TECO, 지표, TE, sqrt, frac, sum, left, right, n-1, Where

[질문 유형 목표]
criteria 0개, definition 2개, comparison 2개, application 2개

[이번 호출에서 허용되는 질문 유형]
definition, comparison, application

[이번 호출의 생성 목표]
definition 2개, comparison 2개, application 2개를 우선 맞춘다.

규칙:
- 질문은 짧고 단일 쟁점 중심
- 문서 범위를 벗어난 질문 금지
- 제목을 그대로 복붙한 문장 금지
- 일반 금융상식, 투자조언, 시장전망 질문 금지
- 업로드 문서를 보지 않고도 일반 금융 상식만으로 답할 수 있는 질문은 생성하지 않는다
- 답이 표 전체 요약이나 장문 설명이 되도록 만드는 질문은 금지하고, 특정 기준 하나만 묻는 질문으로 만든다
- 질문 하나에는 하나의 판단축만 남긴다. 정의와 기준, 기준과 예외를 한 문장에 함께 묻지 않는다
- 정답은 문서의 핵심 정보와 의미 연결을 위한 최소 설명만으로 완결될 수 있어야 한다
- 정답이 자연스러운 문장으로 연결되더라도 새로운 정보 추가 없이 답할 수 있어야 한다
- 질문이 새로운 판단 기준, 질문에 없는 대상, 불필요한 비교를 답변에 끌어오게 만들면 폐기한다
- definition 질문은 정의문 하나로 직접 답할 수 있어야 하며 절차·비교·예외를 동시에 요구하지 않는다
- criteria 질문은 판단 기준만 묻게 하고 적용 절차나 다른 대상 비교를 함께 요구하지 않는다
- comparison 질문은 필요한 비교축만 묻게 하고 각 대상의 정의나 절차 설명까지 요구하지 않는다
- application 질문은 적용 순서나 전환 조건을 묻되 정의/비교를 한 문장에 함께 묻지 않는다
- 질문 유형은 definition, criteria, comparison, application 중 하나만 사용
- 허용되지 않은 질문 유형은 절대 생성하지 않는다
- 최대 6개까지만 생성

생성 후 자체 점검:
- 이 질문은 문서의 특정 표, 기준, 정의, 절차가 없으면 답하기 어려운가?
- 이 질문에 일반 금융 상식으로 그럴듯하게 답할 수 있으면 폐기한다
- 질문이 길거나 복합적이면 더 짧은 한 쟁점 질문으로 다시 쓴다
- 이 질문에 답하려고 할 때 질문에 없는 대상/비교/예외를 덧붙이게 되면 폐기한다

반환 형식:
순수 JSON 배열만 출력한다.
[
  {
    "question": "질문 내용",
    "qa_type": "definition|criteria|comparison|application",
    "question_template": "템플릿 이름"
  }
]

```

---

## Stage
answers

## Record ID
v5_q_0089

## Model
gpt-5.4-nano

## API Base
https://api.openai.com/v1

## Extra
```json
{
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee",
  "chapter": "X.7. 유사펀드 측정",
  "section": "5) 선정지표",
  "subsection": "",
  "seed_title": "5) 선정지표",
  "question": "선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.",
  "answer_policy": "structured_long"
}
```

## System Prompt
```text
제로인은 펀드평가사로 펀드평가의 방법론을 가지고 있습니다.
당신은 제로인 방법론에 근거해 답변하는 도메인 어시스턴트다.
반드시 한국어로만 답한다.
업로드된 문서에 근거가 없는 기준, 수치, 예외를 만들지 않는다.
질문에 필요한 최소 범위까지만 답한다.
문서 밖 일반 지식으로 빈칸을 메우지 않는다.
문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소한의 설명만 허용한다.
이 최소 설명은 용어를 풀거나 생략된 주어와 관계를 잇는 수준에 한한다.
답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다.
답변은 짧고 구조적으로 유지한다.
문서의 핵심 용어와 판단 기준어는 가능하면 유지한다.
질문이 묻지 않은 다른 판단 기준으로 확장하지 않는다.
새로운 판단 기준, 규칙, 예외, 질문에 없는 대상이나 비교는 추가하지 않는다.
출처/원문/문서/장/절을 직접 언급하지 않는다.
일반 금융 상식으로 확장하지 않는다.
```

## User Prompt
```text
[질문]
선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.

[답변 스타일]
구조 보존형 기준/적용 답변으로 3~6개 불릿까지 허용한다. 질문에 필요한 조건, 적용 범위, 예외와 문맥 연결용 최소 설명만 남기고 표 전체 해설이나 긴 배경설명은 금지한다.
- 답변 길이는 공백 포함 최대 1400자 이내
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소 설명만 허용한다
- 이 최소 설명은 용어 풀이와 생략된 주어/관계 보완 수준에 한한다
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다
- 새로운 판단 기준, 규칙, 예외, 질문에 없는 대상/비교는 추가하지 않는다
- 적용형: 답변은 적용 순서, 적용 조건, 전환 규칙만 제시한다
- 적용형: 정의 재설명이나 불필요한 비교는 포함하지 않는다
- 질문에 직접 필요한 기준만 남기고 예시, 배경설명, 장황한 열거는 줄인다
- 제목, 번호 섹션, 마크다운 헤더(예: ##, 1) )는 쓰지 않는다
```

---

## Assistant Response
```text
- 매월 2일 기준으로 전월부터 이전 1년 동안의 주간수익률 자료를 이용해 유사펀드를 선정한다   
- 선정 대상은 문서에 제시된 유사펀드 선정 대상 유형(공모/사모, 클래스/모펀드 모두 포함)에 한정한다   
- 선정지표(TECO) 산출을 위해, 각 대상펀드 i와 타겟펀드 j의 수익률 시계열을 기준으로 TE, 공분산/상관을 계산해 TECO_{i,f}^T 를 산출한다   
- TECO_{i,f}^T 를 TECO_{i,f}^T_score 로 전환한다(TECO_{i,f}^T_score= 100 - 100×(TECO_{i,f}^T/최댓값))   
- 결과 제시는 대상펀드 중 공모 펀드를 대상으로 TECO score가 높은 순으로 유사성 지표를 나열한다
```

## Response Extra
```json
{
  "answer_status": "generated",
  "answer_issues": [],
  "answer_policy": "structured_long",
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee"
}
```

---

## Stage
answers

## Record ID
v5_q_0089

## Model
gpt-5.4-nano

## API Base
https://api.openai.com/v1

## Extra
```json
{
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee",
  "chapter": "X.7. 유사펀드 측정",
  "section": "5) 선정지표",
  "subsection": "",
  "seed_title": "5) 선정지표",
  "question": "선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.",
  "answer_policy": "structured_long"
}
```

## System Prompt
```text
제로인은 펀드평가사로 펀드평가의 방법론을 가지고 있습니다.
- 당신은 제로인 방법론에 근거해 답변하는 도메인 어시스턴트다.
- 반드시 한국어로만 답한다.
- 답변은 질문에 직접 필요한 범위를 넘기지 않되, 독립적으로 이해 가능하도록 필요한 짧은 전제 설명은 포함할 수 있다.
- 답변은 독립적으로 이해 가능해야 한다. 하지만 그 이해 가능성을 이유로 새 정보를 만들면 안 된다.
- 답변은 불필요하게 짧게 끊지 말고, 구조적으로 정리하되 필요한 연결 문장은 남긴다.
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소한의 설명만 허용한다.
- 이 최소 설명은 용어를 풀거나 생략된 주어와 관계를 잇는 수준, 또는 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다.
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다.
- 질문이 묻지 않은 다른 판단 기준으로 확장하지 않는다.
- 출처/원문/문서/장/절을 직접 언급하지 않는다.
- 일반 금융 상식으로 확장하지 않는다.
- **반드시 답변은 독립적으로 이해 가능해야 한다.**
```

## User Prompt
```text
[질문]
선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.

[답변 스타일]
구조 보존형 기준/적용 답변으로 3~6개 불릿까지 허용한다. 질문이 가리키는 대상이나 절차의 맥락을 짧게 잡아 준 뒤, 필요한 조건, 적용 범위, 예외만 남긴다. 표 전체 해설이나 문서 밖 배경설명은 금지한다.
- 답변 길이는 공백 포함 최대 1400자 이내
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소 설명만 허용한다
- 이 최소 설명은 용어 풀이, 생략된 주어/관계 보완, 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다
- 답변은 독립적으로 이해 가능해야 하지만, 그 이해 가능성을 이유로 새 정보를 만들면 안 된다
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다
- 새로운 판단 기준, 규칙, 예외, 질문에 없는 대상/비교는 추가하지 않는다
- 적용형: 답변은 적용 순서, 적용 조건, 전환 규칙만 제시한다
- 적용형: 정의 재설명이나 불필요한 비교는 포함하지 않는다
- 질문에 직접 필요한 기준만 남기되, 새 정보가 아닌 범위에서 짧은 전제 설명은 허용한다
- 예시, 장황한 배경설명, 불필요한 열거는 줄인다
- 제목, 번호 섹션, 마크다운 헤더(예: ##, 1) )는 쓰지 않는다
```

---

## Stage
answers

## Record ID
v5_q_0089

## Model
gpt-5.4-nano

## API Base
https://api.openai.com/v1

## Extra
```json
{
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee",
  "chapter": "X.7. 유사펀드 측정",
  "section": "5) 선정지표",
  "subsection": "",
  "seed_title": "5) 선정지표",
  "question": "선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.",
  "answer_policy": "structured_long"
}
```

## System Prompt
```text
제로인은 펀드평가사로 펀드평가의 방법론을 가지고 있습니다.
- 당신은 제로인 방법론에 근거해 답변하는 도메인 어시스턴트다.
- 반드시 한국어로만 답한다.
- 답변은 질문에 직접 필요한 범위를 넘기지 않되, 독립적으로 이해 가능하도록 필요한 짧은 전제 설명은 포함할 수 있다.
- 답변은 독립적으로 이해 가능해야 한다. 하지만 그 이해 가능성을 이유로 새 정보를 만들면 안 된다.
- 답변은 불필요하게 짧게 끊지 말고, 구조적으로 정리하되 필요한 연결 문장은 남긴다.
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소한의 설명만 허용한다.
- 이 최소 설명은 용어를 풀거나 생략된 주어와 관계를 잇는 수준, 또는 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다.
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다.
- 질문이 묻지 않은 다른 판단 기준으로 확장하지 않는다.
- 출처/원문/문서/장/절을 직접 언급하지 않는다.
- 일반 금융 상식으로 확장하지 않는다.
- **반드시 답변은 독립적으로 이해 가능해야 한다.**
```

## User Prompt
```text
[질문]
선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.

[답변 스타일]
구조 보존형 기준/적용 답변으로 3~6개 불릿까지 허용한다. 질문이 가리키는 대상이나 절차의 맥락을 짧게 잡아 준 뒤, 필요한 조건, 적용 범위, 예외만 남긴다. 표 전체 해설이나 문서 밖 배경설명은 금지한다.
- 답변 길이는 공백 포함 최대 1400자 이내
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소 설명만 허용한다
- 이 최소 설명은 용어 풀이, 생략된 주어/관계 보완, 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다
- 답변은 독립적으로 이해 가능해야 하지만, 그 이해 가능성을 이유로 새 정보를 만들면 안 된다
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다
- 새로운 판단 기준, 규칙, 예외, 질문에 없는 대상/비교는 추가하지 않는다
- 적용형: 답변은 적용 순서, 적용 조건, 전환 규칙만 제시한다
- 적용형: 정의 재설명이나 불필요한 비교는 포함하지 않는다
- 질문에 직접 필요한 기준만 남기되, 새 정보가 아닌 범위에서 짧은 전제 설명은 허용한다
- 예시, 장황한 배경설명, 불필요한 열거는 줄인다
- 제목, 번호 섹션, 마크다운 헤더(예: ##, 1) )는 쓰지 않는다
```

---

## Assistant Response
```text
유사펀드 선정을 위해 선정지표(TECO)를 적용하는 기본 순서는 아래와 같다.

- 적용 1: 유사펀드 선정 주기/자료를 먼저 확정한다. 매월 2일 기준으로 전월부터 이전 1년 동안의 주간수익률 자료를 이용해 산출한다. 
- 적용 2: 유사펀드 후보의 적용 범위를 정한다. 통합채권형/통합주식형/통합혼합형/부동산 등/ELF 등 해당 유형(공모·사모, 클래스·모펀드 포함)에서 선정하되, 글로벌장기미분류·글로벌신규설정펀드·장기미분류·영구미분류·신규설정펀드는 제외한다. 
- 적용 3: 선정지표(TECO)를 계산한다. 타겟펀드와 대상펀드의 주간수익률 차이로 TE를 구하고, 공분산/표준편차로 상관계수 ρ를 산출한 뒤, TECO = TE × (2 − ρ)로 확정한다. 
- 전환 4: TECO를 점수(TECO_score)로 변환한다. TECO_score = 100 − 100 × (TECO / max(TECO))이며, 점수화 시에는 TECO 상위 5% 관측치를 제외한 MAX로 점수의 분모를 산출하고, 상위 5%는 음(-)점이 될 수 있어도 0점으로 처리한다.  
- 전환 5: 산출된 점수를 활용해 순서를 만든다. 웹 제시는 대상펀드 중 공모 펀드만 제시하고, TECO 점수가 높은 순으로 나열한다.
```

## Response Extra
```json
{
  "answer_status": "generated",
  "answer_issues": [],
  "answer_policy": "structured_long",
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee"
}
```

---

## Stage
answers

## Record ID
v5_q_0089

## Model
gpt-5.4-nano

## API Base
https://api.openai.com/v1

## Extra
```json
{
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee",
  "chapter": "X.7. 유사펀드 측정",
  "section": "5) 선정지표",
  "subsection": "",
  "seed_title": "5) 선정지표",
  "question": "선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.",
  "answer_policy": "structured_long"
}
```

## System Prompt
```text
제로인은 펀드평가사로 펀드평가의 방법론을 가지고 있습니다.
- 당신은 제로인 방법론에 근거해 답변하는 도메인 어시스턴트다.
- 반드시 한국어로만 답한다.
- 답변은 질문에 직접 필요한 범위를 넘기지 않되, 독립적으로 이해 가능하도록 필요한 짧은 전제 설명은 포함할 수 있다.
- 답변은 독립적으로 이해 가능해야 한다. 하지만 그 이해 가능성을 이유로 새 정보를 만들면 안 된다.
- 답변은 불필요하게 짧게 끊지 말고, 구조적으로 정리하되 필요한 연결 문장은 남긴다.
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소한의 설명만 허용한다.
- 이 최소 설명은 용어를 풀거나 생략된 주어와 관계를 잇는 수준, 또는 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다.
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다.
- 질문이 묻지 않은 다른 판단 기준으로 확장하지 않는다.
- 출처/원문/문서/장/절을 직접 언급하지 않는다.
- 일반 금융 상식으로 확장하지 않는다.
- **반드시 답변은 독립적으로 이해 가능해야 한다.**
```

## User Prompt
```text
[질문]
선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.

[답변 스타일]
구조 보존형 기준/적용 답변으로 3~6개 불릿까지 허용한다. 질문이 가리키는 대상이나 절차의 맥락을 짧게 잡아 준 뒤, 필요한 조건, 적용 범위, 예외만 남긴다. 표 전체 해설이나 문서 밖 배경설명은 금지한다.
- 답변 길이는 공백 포함 최대 1400자 이내
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소 설명만 허용한다
- 이 최소 설명은 용어 풀이, 생략된 주어/관계 보완, 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다
- 답변은 독립적으로 이해 가능해야 하지만, 그 이해 가능성을 이유로 새 정보를 만들면 안 된다
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다
- 새로운 판단 기준, 규칙, 예외, 질문에 없는 대상/비교는 추가하지 않는다
- 적용형: 답변은 적용 순서, 적용 조건, 전환 규칙만 제시한다
- 적용형: 정의 재설명이나 불필요한 비교는 포함하지 않는다
- 질문에 직접 필요한 기준만 남기되, 새 정보가 아닌 범위에서 짧은 전제 설명은 허용한다
- 예시, 장황한 배경설명, 불필요한 열거는 줄인다
- 제목, 번호 섹션, 마크다운 헤더(예: ##, 1) )는 쓰지 않는다
```

---

## Assistant Response
```text
- 1) TECO(TECO 지표값)의 입력값을 구성한다. 기간 T의 관측치에 대해 펀드 i 수익률 Rt^i와 타겟펀드 수익률 Rt^{j’}의 차이를 제곱해 평균하고(관측치 개수 n 사용) TE_{i,f}^T 를 산출한다   
- 2) TE를 상관성 반영 지표로 전환한다. 같은 기간 T에서 계산한 상관계수(ρ_{i,f}^T)를 이용해 TECO_{i,f}^T = TE_{i,f}^T × (2 − ρ_{i,f}^T)로 변환한다   
- 3) TECO_{i,f}^T 를 점수로 전환(정규화)한다. TECO_{i,f}^T_score = 100 − 100 × (TECO_{i,f}^T / max(TECO_{i,f}^T)) 를 적용한다   
- 4) 전환 규칙(예외/조건)을 적용한다. 점수화 시 max(TECO_{i,f}^T)는 TECO 값이 높은 순으로 상위 5% 관측치는 제외한 뒤 산출하며, 상위 5% 관측치는 점수 계산 과정에서 음(-)이 나올 수 있어도 0점으로 처리한다
```

## Response Extra
```json
{
  "answer_status": "generated",
  "answer_issues": [],
  "answer_policy": "structured_long",
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee"
}
```

---

## Assistant Response
```text
- 대상펀드(수익률 i)와 타겟펀드(수익률 j’)의 t시점 수익률 차이를 이용해 TECO 산출에 필요한 TE를 먼저 계산한다(TE 계산식에 ).  
- 이후 TE에 상관계수(ρ)를 반영해 TECO를 전환한다: TECO( i,f )^T = TE( i,f )^T × (2 − ρ( i,f )^T) .  
- TECO 점수화에서 전환 기준이 되는 MAX는 ‘TECO 값이 높은 순으로 상위 5%에 속하는 관측치’를 제외한 상태에서 산출한다(적용 조건) .  
- TECO를 TECO_score로 전환한다: TECO_score = 100 − 100 × (TECO / MAX(TECO)) .  
- 점수 처리 규칙으로, 위 전환 과정에서 음(-)의 점수가 발생하면 0점으로 처리한다(전환 규칙) .
```

## Response Extra
```json
{
  "answer_status": "generated",
  "answer_issues": [],
  "answer_policy": "structured_long",
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee"
}
```

---

## Stage
answers

## Record ID
v5_q_0089

## Model
gpt-5.4-nano

## API Base
https://api.openai.com/v1

## Extra
```json
{
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee",
  "chapter": "X.7. 유사펀드 측정",
  "section": "5) 선정지표",
  "subsection": "",
  "seed_title": "5) 선정지표",
  "question": "선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.",
  "answer_policy": "structured_long"
}
```

## System Prompt
```text
제로인은 펀드평가사로 펀드평가의 방법론을 가지고 있습니다.
- 당신은 제로인 방법론에 근거해 답변하는 도메인 어시스턴트다.
- 반드시 한국어로만 답한다.
- 답변은 질문에 직접 필요한 범위를 넘기지 않되, 독립적으로 이해 가능하도록 필요한 짧은 전제 설명은 포함할 수 있다.
- 답변은 독립적으로 이해 가능해야 한다. 하지만 그 이해 가능성을 이유로 새 정보를 만들면 안 된다.
- 답변은 불필요하게 짧게 끊지 말고, 구조적으로 정리하되 필요한 연결 문장은 남긴다.
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소한의 설명만 허용한다.
- 이 최소 설명은 용어를 풀거나 생략된 주어와 관계를 잇는 수준, 또는 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다.
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다.
- 질문이 묻지 않은 다른 판단 기준으로 확장하지 않는다.
- 출처/원문/문서/장/절을 직접 언급하지 않는다.
- 일반 금융 상식으로 확장하지 않는다.
- **반드시 답변은 독립적으로 이해 가능해야 한다.**
```

## User Prompt
```text
[질문]
선정지표를 적용하는 기본 순서를 하나의 기준으로 제시하시오.

[답변 스타일]
구조 보존형 기준/적용 답변으로 3~6개 불릿까지 허용한다. 질문이 가리키는 대상이나 절차의 맥락을 짧게 잡아 준 뒤, 필요한 조건, 적용 범위, 예외만 남긴다. 표 전체 해설이나 문서 밖 배경설명은 금지한다.
- 답변 길이는 공백 포함 최대 1400자 이내
- 문서에 없는 내용은 추가하지 않되, 의미 연결을 위한 최소 설명만 허용한다
- 이 최소 설명은 용어 풀이, 생략된 주어/관계 보완, 질문의 대상을 짧게 다시 잡아 주는 수준에 한한다
- 답변은 독립적으로 이해 가능해야 하지만, 그 이해 가능성을 이유로 새 정보를 만들면 안 된다
- 답변은 자연스러운 문장으로 연결하되, 새로운 정보 추가 없이 기존 내용을 매끄럽게 표현한다
- 새로운 판단 기준, 규칙, 예외, 질문에 없는 대상/비교는 추가하지 않는다
- 적용형: 답변은 적용 순서, 적용 조건, 전환 규칙만 제시한다
- 적용형: 정의 재설명이나 불필요한 비교는 포함하지 않는다
- 질문에 직접 필요한 기준만 남기되, 새 정보가 아닌 범위에서 짧은 전제 설명은 허용한다
- 예시, 장황한 배경설명, 불필요한 열거는 줄인다
- 제목, 번호 섹션, 마크다운 헤더(예: ##, 1) )는 쓰지 않는다
```

---

## Assistant Response
```text
- 적용 조건(산출 자료/범위): 유사펀드 선정은 매월 2일에 전월부터 이전 1년 동안의 주간수익률 자료로 산출하며, TE/TECO 계산은 측정기간 T의 수익률을 사용하고 데이터 개수는 n으로 둔다    
- 적용 순서 1(TE 산출): 대상펀드 i와 타겟펀드 j’의 수익률 차이 제곱합을 이용해 TE_{i,j}^T를 계산한다   
- 적용 순서 2(상관계수 산출): 대상펀드 i와 타겟펀드 f의 수익률 공분산/표준편차를 이용해 ρ_{i,f}^T를 계산한다   
- 적용 순서 3(TECO 산출): TECO_{i,f}^T = TE_{i,f}^T × (2 − ρ_{i,f}^T)로 전환해 TECO 지표값을 만든다   
- 전환 규칙(TECO→TECO_score 점수화): TECO_score는 100 − 100×(TECO_{i,f}^T / max(TECO_{i,f}^T))로 산출하되, 점수화 시 max(TECO) 계산에서 TECO 값 상위 5% 관측치는 제외해 MAX를 산출하고, 그 결과 상위 5% 구간의 음(-) 점수는 0점으로 처리한다
```

## Response Extra
```json
{
  "answer_status": "generated",
  "answer_issues": [],
  "answer_policy": "structured_long",
  "vector_store_id": "vs_68a80414c938819189ac784ba37c10ee"
}
```
